import styled from "styled-components";
import theme from "../theme/theme";

export const StyledHeaderStripe = styled.div`
    background-color: ${theme.palette.primary.dark};
    height: 50px;
    width: 100%;
`;